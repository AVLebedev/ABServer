<Window x:Class="ABServer.ClientsViewForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="Просмотр клиентов" Height="459" Width="Auto" Loaded="Window_Loaded">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <DataGrid ItemsSource="{Binding dbSet, 
                        Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged}" 
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="dataGrid" AutoGenerateColumns="True" Margin="0,0,0,0" CellEditEnding="dataGrid_CellEditEnding"/>
        <Button Name="saveBtn" Content="Сохранить" HorizontalAlignment="Right" VerticalAlignment="Top" Width="96" Height="25" FontSize="13" Click="saveBtn_Click"/>

    </Grid>
</Window>
